# Toggle data source adapters
data_sources:
  use_mock: true         # set to false when you wire real fetchers
  games_date: auto       # "auto" = today (UTC). Or fix like "2025-11-04".
windows:
  ev_minutes_min: 300    # minutes needed before trusting player-specific rates
  pp_minutes_min: 60
  roll_games: 10
priors:
  sog_per60_forward: 7.2
  sog_per60_defense: 4.1
  g_per60_forward: 1.0
  g_per60_defense: 0.4
  a1_per60_forward: 0.9
  a1_per60_defense: 0.5
  a2_per60_forward: 0.5
  a2_per60_defense: 0.3
shrinkage:
  tau_ev: 400            # minutes to half weight the prior
  tau_pp: 120
pace:
  use_geometric_mean: true
goalie:
  beta_gsax: -0.35
  default_gsax60: 0.0
early_pp:
  q_draw_first10: 0.18
report:
  top_n: 10
  include_rationale: true
publish:
  branch: gh-pages
  commit_message: "chore: daily nhl picks"
  site_title: "Daily NHL Projections & Picks"
