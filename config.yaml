publish:
  site_title: NHL Picks — Projections

report:
  top_n: 10
  sog_over_line: 3

nhl:
  last_n: 7        # recent-window for blending (e.g., 5 or 7)
  w_recent: 0.55   # weight on recent form in the blend [0..1]

pace:
  use_geometric_mean: true

goalie:
  beta_gsax: -0.35  # (kept for later; goalies are neutral in NHL.com flow)

priors:
  sog_per60_forward: 7.2
  sog_per60_defense: 4.1
  g_per60_forward: 1.0
  g_per60_defense: 0.4
  a1_per60_forward: 0.9
  a1_per60_defense: 0.5
  a2_per60_forward: 0.5
  a2_per60_defense: 0.3

shrinkage:
  tau_ev: 400
  tau_pp: 120
